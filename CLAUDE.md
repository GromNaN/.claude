# Global rules

## File conventions

- All text files must end with a newline (no missing newline at EOF).

## PHP code style

For Doctrine and MongoDB PHP projects, run `phpcbf` before committing to auto-fix code style, then verify with `phpcs`. These tools must be run before any commit.